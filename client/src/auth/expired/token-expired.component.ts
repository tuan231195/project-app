import {TokenExpiredController} from './token-expired.controller';

const template = require('./token-expired.component.html');

export const tokenExpiredComponent = {
    controller: TokenExpiredController,
    controllerAs: 'TokenExpiredCtrl',
    template,
};